name: Build APK

# APK w trybie RELEASE (AOT — apka działa dużo płynniej niż debug/JIT),
# podpisany commitowanym debug.keystore (celowo: stała sygnatura =
# aktualizacje nadpisują poprzednie instalacje bez odinstalowywania;
# to apka rodzinna dystrybuowana poza sklepem, nie potrzebuje
# prawdziwego release keystore).
#
# Trigger: workflow_dispatch (manualnie) ORAZ automatycznie po każdym
# push do main który zmienia kod apki. Dzięki temu po każdym merge
# APK buduje się sam i ląduje pod stałym linkiem release'a.

on:
  # Manualny trigger z UI (dalej dostępny).
  workflow_dispatch:
  # Auto-build przy push do main, ALE tylko gdy zmienił się kod apki —
  # pomijamy zmiany dotyczące wyłącznie dokumentacji / skilli / migracji
  # SQL (nie wpływają na APK), żeby nie palić minut CI.
  push:
    branches: [main]
    paths:
      - 'lib/**'
      - 'android/**'
      - 'assets/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - '.github/workflows/build-apk-debug.yml'

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    # AOT release dla 3 ABI trwa dłużej niż debug — stąd 35 min.
    timeout-minutes: 35
    # contents: write — krok „Publish APK" tworzy/aktualizuje release „apk".
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      # Pin identyczny z ci.yml — bez pinu „stable" potrafi dać nowszy SDK
      # niż ten, na którym przeszło CI (23.08: CI na 3.41.9 zielone,
      # build na 3.47.1 padł na API usuniętym w nowszym Flutterze).
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.9'
          channel: stable
          cache: true

      # Pliki gradle-wrapper.jar / gradlew / local.properties są .gitignored
      # (każdy clone Flutter dogenerowuje je sam). To wystarczy żeby reszta
      # zatwierdzona w repo (build.gradle.kts, MainActivity.kt, styles, mipmapy,
      # AndroidManifest.xml) miała kompletny scaffolding do buildu.
      #
      # Retry: pub.dev miewa transient socket errors (exit code 69).
      # 3 próby z exponential backoff — wystarczy w 99% przypadków.
      - name: Bootstrap gradle wrapper + local.properties
        run: |
          for attempt in 1 2 3; do
            if flutter create --platforms=android \
                 --org=pl.naszbudzetdomowy \
                 --project-name=nasz_budzet_domowy .; then
              echo "✅ flutter create OK (attempt $attempt)"
              break
            fi
            if [ "$attempt" = "3" ]; then
              echo "❌ flutter create padło 3x — pub.dev nieosiągalne"
              exit 1
            fi
            wait=$((attempt * 15))
            echo "⏳ flutter create padło (attempt $attempt) — czekam ${wait}s"
            sleep $wait
          done

      - name: Install dependencies
        run: |
          for attempt in 1 2 3; do
            if flutter pub get; then
              echo "✅ pub get OK (attempt $attempt)"
              break
            fi
            if [ "$attempt" = "3" ]; then
              echo "❌ pub get padło 3x — pub.dev nieosiągalne"
              exit 1
            fi
            wait=$((attempt * 15))
            echo "⏳ pub get padło (attempt $attempt) — czekam ${wait}s"
            sleep $wait
          done

      # AGP 8 nie akceptuje atrybutu `package="..."` w AndroidManifest.xml
      # — namespace MUSI być w build.gradle. Stare pluginy Flutter (Vosk,
      # niektóre starsze wersje) wciąż mają legacy `package="..."` i build
      # pada na ich processDebugManifest. Stripujemy atrybut ze wszystkich
      # plugin-manifestów w pub-cache. Modyfikacja pub-cache jest OK na
      # świeżym CI runnerze (każdy run startuje od zera).
      - name: Strip legacy package="..." z manifestów starych pluginów (AGP 8)
        run: |
          set -e
          stripped=0
          while IFS= read -r f; do
            if grep -q ' package="' "$f"; then
              echo "Stripping legacy package= from: $f"
              sed -i 's/[[:space:]]\+package="[^"]*"//' "$f"
              stripped=$((stripped + 1))
            fi
          done < <(find ~/.pub-cache/hosted -name AndroidManifest.xml -path "*/android/*")
          echo "Stripped $stripped manifest(s)."

      - name: Build release APK
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          if [ -z "$SUPABASE_URL" ] || [ -z "$SUPABASE_ANON_KEY" ]; then
            echo "❌ Brak sekretów SUPABASE_URL / SUPABASE_ANON_KEY"
            echo "Dodaj je w Settings → Secrets and variables → Actions"
            exit 1
          fi
          # --release: kompilacja AOT — apka wyraźnie płynniejsza i mniejsza
          # niż debug/JIT. --split-per-abi: osobny APK per architektura
          # (libvosk/Kaldi nie dubluje się 3x w jednym pliku).
          flutter build apk --release --split-per-abi \
            --dart-define=SUPABASE_URL="$SUPABASE_URL" \
            --dart-define=SUPABASE_ANON_KEY="$SUPABASE_ANON_KEY" \
            --dart-define=APP_VERSION=release-${{ github.sha }}

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: nasz-budzet-domowy-apk
          path: build/app/outputs/flutter-apk/app-*-release.apk
          if-no-files-found: error
          retention-days: 30

      # Stały link do pobrania na telefonie: surowy plik .apk w rolling
      # release „apk" (artefakt Actions to zip i wymaga zalogowania).
      # Najnowsza wersja jest zawsze pod:
      # https://github.com/adrianbryniarski-tech/Aplikacja-Nasz-budzet-domowy/releases/latest/download/nasz-budzet-domowy.apk
      #
      # Publikujemy wariant arm64-v8a: apka wymaga Androida 11+ (minSdk 30),
      # a te urządzenia są 64-bitowe. Pozostałe warianty (v7a, x86_64) są
      # w artefakcie Actions, gdyby kiedyś były potrzebne.
      - name: Publish APK to rolling release „apk"
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e
          cp build/app/outputs/flutter-apk/app-arm64-v8a-release.apk \
            nasz-budzet-domowy.apk
          notes="Najnowsza wersja apki (release, podpis debug keystore). Zaktualizowano: $(date -u '+%Y-%m-%d %H:%M UTC'), commit ${GITHUB_SHA::7}. Przed instalacją odinstaluj poprzednią wersję, jeśli instalator zgłosi konflikt podpisu."
          if gh release view apk >/dev/null 2>&1; then
            gh release edit apk --title "Najnowszy APK" --notes "$notes" --latest
          else
            gh release create apk --title "Najnowszy APK" --notes "$notes" --latest
          fi
          gh release upload apk nasz-budzet-domowy.apk --clobber
          echo "✅ APK w release: https://github.com/${GITHUB_REPOSITORY}/releases/latest/download/nasz-budzet-domowy.apk"
